server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        # In production, proxy to backend services
        # For now, frontend calls backends directly via CORS
        return 404;
    }
}
